@model List<Category>

@{
    if (@Model.Count == 0)
    {
        <div>HEŞBİŞEY TAPMADIM QERDEŞ!</div>
    }
    else
    {
        @foreach (var category in @Model)
        {
            <tr class="category-row" data-category-id="@category.id">
                <td>@category.id</td>
                <td>@category.Name</td>
                <td>
                    <a asp-controller="Category" asp-action="Detail" asp-route-id="@category.id" class="btn btn-sm btn-primary">Details</a>
                    <a class="btn btn-sm btn-success" asp-controller="Category" asp-action="Update" asp-route-id="@category.id">Update</a>
                    <a class="btn btn-sm btn-danger delete-category-button">Delete</a>
                </td>
            </tr>
        }
    }
}